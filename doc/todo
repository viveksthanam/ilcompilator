bon projet: 

le langage source:
mecanisme de declaration locale (principe pile-accolade)
typage minimum (int bool float)
verification de type associee (addition bool float impossible)
structures de controle (au moins if et while)
expressions arbitrairement complexes (3*x+(4/y))

code cible:
3 @ (pas de declaration locale, instructions a 3@, goto pour les branch)

jeu de test:
simple
un pour chaque fonctionnalite
commentaire du resultat a chaque fois

tres bon projet (trucs a creuser):
declaration/manipulation/typage pointeurs
production de code online (a definir)
evaluation paresseuse des bool
techniques optimisation (boucles inutiles, etc)
autre (le contacter)

